// Este archivo contiene el código JavaScript para la página web. Se utiliza para agregar interactividad y funcionalidades dinámicas.

const validarUsuario = (valor,usuario)=>{
    valor = prompt( "cual es el valor ?")
    usuario = []
    for(i = 0; i<5; i++){
        usuario = [prompt("dime tu nombre")]
    }
}
